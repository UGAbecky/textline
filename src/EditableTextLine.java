package edu.uga.cs1302.txtbuff;

/**
 * The following class represents a driver class to implement the following
 * {@code TextLine}.
 *
 */

public class EditableTextLine() {

    /*
     * Empty constructor
     */

    public EditableTextLine() {

    }

    /**
     * The following constructor object initializes {@code EditableTextLine} to contain
     * the same number of characters as the argument parameter {@code line}.
     *
     * @param line argument being passed in the array.
     */

    public EditableTextLine (String line) {
        text = new String[other.size()];
        for (int i = 0; i < other.size(); i++) {
            text[i] = other.get(i);
        } // for
    }

} //EditableTextLine
